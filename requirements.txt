# Core requirements with Python 3.13 compatibility
Flask>=2.3.0
Werkzeug>=2.3.0
gunicorn>=21.0.0
python-dotenv>=1.0.0

# Computer Vision - headless versions to save space
opencv-python-headless>=4.8.0
numpy>=1.24.0
cmake>=3.27.0  # Required for dlib build
dlib>=19.24.0
face-recognition>=1.3.0

# Database
pymongo[srv]>=4.5.0

# Required dependencies
Pillow>=10.0.0  # Required for face_recognition
pandas>=2.0.0  # Required for attendance tracking
